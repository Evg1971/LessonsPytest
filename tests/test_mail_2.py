# Импортируем библиотеку pytest для написания и запуска тестов
import pytest

# Фикстура (fixture) — функция, которая может использоваться для подготовки данных или состояния перед тестами.
# Однако в данном коде она не подключена ни к одному тесту.
@pytest.fixture
def set_up():
    # Выводим сообщение о регистрации пользователя
    print('Пользователь зарегистрировался')

# Тестовая функция, которая проверяет отправку письма.
# Фикстура set_up здесь не используется.
def test_sending_mail_1():
    # Выводим сообщение об отправке письма
    print('Письмо отправлено')

# Тестовая функция, которая также проверяет отправку письма.
# Фикстура set_up здесь тоже не используется.
def test_sending_mail_2():
    # Выводим сообщение об отправке письма
    print('Письмо отправлено')
